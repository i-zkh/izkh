.navbar.navbar-default.navbar-static-top role="navigation" style="position: fixed; z-index: 10; background-color: white; width: 100%"
  .container
    .row
      .hidden-xs
        = link_to "АйЖКХ", root_path, class: "navbar-brand" 
        p style="padding-top: 15px; float: left"
          | Доступный платежный сервис
          .pad-left= link_to "Профиль", dashboard_index_path, class: controller_name == "dashboard" ? 'active' : ''
          .pad-left= link_to "Аналитика", transactions_path, class: controller_name == "transactions" ? 'active' : ''
          span.pad-left
            | Самара: (846) 336 54 34
      .nav.navbar-nav.navbar-right
        - if current_user
          = current_user.first_name
            |
          = link_to destroy_user_session_path
            |  выйти
        - else
          = link_to new_user_session_path
            | Войти
              